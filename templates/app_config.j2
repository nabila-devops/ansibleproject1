# Jinja2 templating
{# Jinja2 template representing configuration file #}

{{ section_header }}
host_role = {{ host_role }}
max_connections={{max_connections}}
node_ip ={{ ansible_all_ipv4_addresses }}
